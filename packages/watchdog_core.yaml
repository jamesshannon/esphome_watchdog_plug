safe_mode:

preferences:
  flash_write_interval: 0s

logger:
  level: DEBUG
  logs:
    component: NONE
    esp-idf: WARN
    sensor: INFO

api:
  id: api_component
  reboot_timeout: 0s
  actions:
    - action: power_cycle
      then:
        - script.execute:
            id: request_power_cycle
            reason: "api"
            automatic: false
            allow_self_reboot_preflight: false

ota:
  platform: esphome
  password: "c9e550ec104cd83f1fcfef5dc5b108a2"

captive_portal:

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  reboot_timeout: 0s
  power_save_mode: none
  ap:
    ssid: "Plug-Pm-1 Fallback Hotspot"
    password: "tpHfmEHr135L"
    ap_timeout: 30min

http_request:
